pipeline {
  agent {
    docker {
      image 'ruby:2.4'
    }
  }
  stages {
    stage('Build') {
      agent {
        dockerfile {
          additionalBuildArgs "--pull"
        }
      }
      steps {
        sh "./build.sh ${env.BUILD_NUMBER}"
      }
    }
  }
}
