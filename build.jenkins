pipeline {
  agent {
    docker {
      image 'ruby:2.4'
    }
  }
  stages {
    stage('Build') {
      agent {
        dockerfile {
          additionalBuildArgs "--pull"
        }
      }
      steps {
        ./build.sh "$BUILD_NUMBER"
      }
    }
  }
}
